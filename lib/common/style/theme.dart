import 'package:flutter/material.dart';

import 'index.dart';

class AppTheme {
  static ThemeData light = const MaterialTheme(TextTheme()).light();
  static ThemeData dark = const MaterialTheme(TextTheme()).dark();
}
